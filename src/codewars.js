// // function arrayPlusArray(array1, array2) {
// // 	const sum1 = array1
// // 	const sum2 = array2
// // 	const totalSum =
// // 		array1.reduce((acc, value) => acc + value, 0) +
// // 		array2.reduce((acc, value) => acc + value, 0)
// // 	return totalSum
// // }

// function swapValues(arr) {
// 	var temp = arr[0]
// 	arr[0] = arr[1]
// 	arr[1] = temp
// }

// function facRecursion(value) {
// 	if (value < 0) {
// 		return 0
// 	}
// 	if (value === 1 || value === 0) {
// 		return 1
// 	}
// 	return value * facRecursion(value - 1)
// }

// function integrate(coefficient, exponent) {
// 	return coefficient / (exponent + 1) + 'x^' + (exponent + 1)
// }